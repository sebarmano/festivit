.nested-fields
  = f.input :title
  - if type == "video"
    = f.input :link
  - if type == "song"
    = f.file_field :song
  - if type == "photo"
    = f.file_field :image
  = f.hidden_field :type, :value => type
  = link_to_remove_association "remove attachment", f